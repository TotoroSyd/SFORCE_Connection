const express = require("express");
const retrieveAccountRoute = require("./retrieveAccountRoute");
const router = express.Router();

// router.get("/retrieve", retrieveAccountRoute);
// router.post("/create", createAccountRoute);

module.exports = router;
